# =============================================================================
# Ad Factory - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.

# --- Deployment ---
# DEPLOYMENT_MODE=production   # uncomment for production (enables auth, disables local mounts)
DEPLOYMENT_MODE=development

# --- MongoDB ---
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/ad-factory?retryWrites=true&w=majority
MONGODB_DB_NAME=ad_factory

# --- App Security ---
APP_SECRET_KEY=change-me-in-production-use-a-real-secret
ENCRYPTION_KEY=change-me-32-char-minimum-encryption-key!

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4090/api/auth/google/callback

# --- CORS / Frontend ---
FRONTEND_ORIGIN=http://localhost:4090
CORS_ORIGINS=http://localhost:4090,http://127.0.0.1:4090

# --- Session ---
SESSION_EXPIRE_MINUTES=1440
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Image storage backend ---
# STORAGE_PROVIDER  controls where generated images are uploaded.
#   local      (default) – files stay on disk, for dev/internal testing only
#   cloudinary          – uploads to Cloudinary CDN (required in production)
# STORAGE_PROVIDER=local   # uncomment for dev (files stay on disk)
# For production, set the following instead:
# STORAGE_PROVIDER=cloudinary
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret
