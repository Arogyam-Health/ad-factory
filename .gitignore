# Secrets / environment
.env
.env.*
!.env.example

# Virtual environments
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Leftover local content dirs (not used by Render + Mongo + local agent)
output/
generated_images/
runtime/
dashboard_storage/
trash/
.local-stack/

# Tool directories
.sixth/
.opencode/
graphify-out/

# Python cache in scripts (added from current_working)
scripts/__pycache__/*

# Local tool cache
.commandcode

# React dashboard
node_modules/
# Built SPA is tracked so Render's Python-only build can serve `/`.
# Docker and `npm run build` refresh it.

# Local editor and test caches
.cursor/
.pytest_cache/
